function main (input) {
	let result = '';
	
	input.trim().split('\n')
    	.map(line => line.split(/\s+/))
    	.forEach((line, i) => {
    		let [] = line;
		});

    return result;
}

module.exports = main;